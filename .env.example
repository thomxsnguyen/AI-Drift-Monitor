# Database Configuration
DB_NAME=driftmonitor
DB_USER=driftuser
DB_PASSWORD=driftpass
DB_HOST=postgres
DB_PORT=5432

# API Configuration
NODE_ENV=development
API_PORT=3000
CRON_SCHEDULE=0 * * * *

# Auditor Configuration
AUDITOR_PORT=8000
